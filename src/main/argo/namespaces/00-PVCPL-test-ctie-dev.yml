kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-ctie-dev-pipeline-claim
  namespace: test-ctie-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

